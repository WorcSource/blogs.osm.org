<!DOCTYPE html>
<html>
<head>
  <meta charset='utf-8'>
  <meta name='generator' content='<%= Pluto.generator %>'>

  <title><%= site.title %></title>

  <link rel="alternate" type="application/rss+xml" title="Blog posts (RSS)" href="rss20.xml"/>
  <link rel="alternate" type="application/atom+xml" title="Blog posts (Atom)" href="atom.xml"/>
  <link rel="shortcut icon" href="/favicon.ico">

  <%= stylesheet_link_tag 'css/hacker' %>
</head>
<body>

<div class='container'>

  <div class='masthead'>
    <table>
      <tr>
        <td><h1><%= site.title %></h1></td>
        <td>
          Last Updated:
          <% if site.updated %>
            <%= site.updated.strftime('%A, %d. %B %Y %H:%M') %>
          <% end %>
        </td>
      </tr>
    </table>
  </div>

  <table>
    <% site.items.latest.limit(30).each_with_index do |item, index|
      if item.summary
        item_snippet_plain_text = strip_tags( item.summary )[0..500]
      elsif item.content
        item_snippet_plain_text = strip_tags( item.content )[0..500]
      else
        item_snippet_plain_text = '-/-'
      end
         %>
      <tr>
        <td valign='top' align='right'><%= "#{index + 1}." %></td>
        <td><!-- up voting to be done --> &and;</td>
        <td>
          <%= link_to item.title, item.url, title: item_snippet_plain_text %>
          <span class='item-feed-title'>
        &bull; <%= link_to item.feed.title, item.feed.url %>
       </span>
        <td>
      </tr>
      <tr>
        <td colspan='2'></td>
        <td>
      <span class='item-points'>
        1 point
      </span>
          <span class='item-published'>
         &bull;  <%= item.published.strftime('%A %B %d, %Y @ %H:%M') %>
            &bull;  <%= time_ago_in_words(item.published) %>
      </span>
        </td>
      </tr>
      <% end %><!-- each item -->
  </table>

  <table>
    <tr>
      <td><a href="https://github.com/worcester-space/planet.worcester.space/blob/master/FEEDS.md">Add your blog?</a>
      </td>
    </tr>
  </table>

</div><!-- container -->

</body>
</html>
